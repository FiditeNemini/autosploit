# macOS
.DS_Store
*.swp
*~

# Xcode
build/
DerivedData/
*.xcuserstate
*.xcworkspace/xcuserdata/
*.pbxuser
*.mode1v3
*.mode2v3
*.perspectivev3
xcuserdata/

# Swift Package Manager
.build/
Packages/
Package.resolved

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
*.egg
.venv/
venv/

# IDE
.idea/
.vscode/
*.sublime-*

# Build artifacts
*.app
*.dmg
*.zip
release/

# Secrets
.env
*.key
*.pem

# Internal dev docs (not for public repo)
docs/plans/
# NOTE: only exclude legacy lowercase `tests/` sibling dirs — the Swift
# XCTest target lives at `ExploitBot/Tests/ExploitBotTests/` and MUST
# be tracked so `swift test` works across clones.
/tests/

# Claude Code local rules + internal notes (never commit)
.claude/hookify.*.local.md
.claude/internal/
# Screenshots with test data
assets/creds.png
assets/exploit.png
assets/network.png
assets/osint.png
assets/post.png
assets/recon.png
assets/report.png
assets/stash.png
assets/web.png
# Bundled tool binaries (too large for git, distributed via DMG)
ExploitBot/Resources/tools/

# Local multi-agent coordination (never commit to public repo)
.agent-coordination/
/CLAUDE.md
/AGENTS.md

# Z005: Bundled Swift resources are canonical; do not recreate the dead engine prompt copies.
/ExploitBotEngine/prompts/tabs/

# SQLite WAL/SHM sidecars from starter-cves.db opens
*.db-shm
*.db-wal
